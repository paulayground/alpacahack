# 배운 점

`/dev/tty`: 현재 프로세스가 연결된 터미널

# 풀이 과정 기록

문제 구조상 `stdin`, `stdout`, `stderr`를 사용하지 않고 바로 터미널에서 출력시켜야하는 문제같다고 생각이 들었다.

터미널 창에 바로 띄울 수 있는 방법들을 찾아보다 `/dev/tty`를 찾게 되었고 `flag.txt`를 읽은 `stdout`를 리다이렉션을 통해 `/dev/tty`보내 출력하여 정답을 찾게 되었다.

# 익스플로잇 코드 정리

# 심화 학습 (Deep Dive)

# 참고
